/**
 * Maply's application-specific editing composition boundary.
 *
 * This module will expose live editor state, commands, lifecycle, and autosave
 * while keeping UI rendering and DOM interaction in src/.
 */
export {};
